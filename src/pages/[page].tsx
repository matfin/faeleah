import React from 'react';
import Layout from 'components/layout';

const Page = (): JSX.Element => {
  return (
    <Layout>
      <p>This is a page is it not?</p>
    </Layout>
  );
};

export default Page;
